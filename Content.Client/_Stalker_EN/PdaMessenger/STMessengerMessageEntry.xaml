<BoxContainer xmlns="https://spacestation14.io"
              xmlns:msg="clr-namespace:Content.Client._Stalker_EN.PdaMessenger"
              xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
              xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls"
              Orientation="Vertical"
              HorizontalExpand="True"
              Margin="0 2 0 2">

    <!-- Sender + Timestamp -->
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
        <TextureRect Name="FactionIcon" MinSize="20 20" Stretch="KeepAspectCentered" Margin="0 0 4 0" Visible="False"/>
        <Label Name="SenderLabel" StyleClasses="LabelKeyText"/>
        <Label Name="TimestampLabel" StyleClasses="LabelSubText" Margin="8 0 0 0"/>
        <Control HorizontalExpand="True"/>
        <Button Name="ReplyButton" Text="{Loc 'st-messenger-reply-button'}" MinWidth="50" MinHeight="20"/>
    </BoxContainer>

    <!-- Reply quote (if replying) -->
    <Label Name="ReplyQuote" Visible="False" StyleClasses="LabelSubText" Margin="4 0 0 0"/>

    <!-- Message content -->
    <RichTextLabel Name="ContentLabel" HorizontalExpand="True" Margin="4 2 0 0"/>

    <customControls:HSeparator Margin="0 4 0 0"/>

</BoxContainer>
